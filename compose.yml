services:

  postgres:
    image: postgres:16-alpine
    container_name: codeja-postgres

    environment:
      POSTGRES_DB: empleados
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: